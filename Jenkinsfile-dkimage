pipeline {
    agent {
        docker.withServer('tcp://192.168.64.1:2375') { docker.image 'node:14-alpine' }
    }
    stages {
        stage('Test') {
            steps {
                sh 'node --version'
            }
        }
    }
}
